# YOLO 目标跟踪与可切换 TPT/AVT 门控

本目录实现目标跟踪、TPT 时间持续性门控和 AVT 面积变化门控：

```text
Ultralytics Results -> 候选阈值/忽略区 -> 分类目标跟踪 -> TPT 或 AVT -> TriggerEvent
```

## 直接运行视频

```bash
python run_temporal.py \
  --source input.mp4 \
  --model yolo26s-p2-1024-recall/weights/best.pt \
  --config config/temporal.yaml \
  --temporal tpt \
  --camera-id camera_001 \
  --inference-fps 5 \
  --save output.mp4 \
  --events events.jsonl
```

摄像头输入可使用 `--source 0`，RTSP 可直接传 URL。加 `--show` 可实时显示。每当某条轨迹首次通过门控，程序向标准输出和可选的 JSONL 文件写入一个事件；这个事件就是后续创建视频片段和提交大模型验证任务的入口。

模型的检测置信度在 `model.predict()` 中设为 `0.01`，目的是先保留原始候选，再由 `config/temporal.yaml` 中按类别配置的 `candidate_confidence` 统一过滤。不要把 YOLO 推理阈值调得高于这里的候选阈值，否则后处理无法找回已被 YOLO 丢弃的框。

室内固定机位的小火苗可使用专用 TPT 配置。它不修改模型权重，将 fire 候选阈值降为 0.30，并要求普通候选在最近 5 次采样中命中 3 次；置信度达到 0.80 时走快速通道：

```bash
python run_temporal.py \
  --source video_1.mp4 \
  --config config/temporal_indoor_small_fire.yaml \
  --inference-fps 10 \
  --show
```

现场试调时无需修改 YAML，可用 `--candidate-confidence-fire` 或 `--candidate-confidence-smoke` 临时覆盖。建议 fire 只在 `0.30`、`0.35`、`0.40` 三档中测试；低于 0.30 通常不会让首检更早，只会增加误候选和二级验证负载。

## 切换 TPT 与 AVT

命令行优先级高于 YAML：

```bash
python run_temporal.py --source input.mp4 --temporal tpt
python run_temporal.py --source input.mp4 --temporal avt
```

也可以只修改 `config/temporal.yaml`：

```yaml
temporal_mode: tpt  # 或 avt
```

TPT 实现在 `temporal_gates/tpt.py`，AVT 实现在 `temporal_gates/avt.py`。如果系统运行中切换算法，应重新创建 `FireSmokePostprocessor`，不要把前一种算法的轨迹门控状态带入后一种算法。

## 接入已有推理循环

```python
from dataclasses import replace
from temporal_gates import FireSmokePostprocessor, load_config

config = load_config("config/temporal.yaml")
config = replace(config, temporal_mode="avt")  # 改为 "tpt" 即使用 TPT
postprocessor = FireSmokePostprocessor(config)

# 每次实际执行小模型推理后都要调用，包括本次结果没有检测框的情况。
result = model.predict(frame, conf=0.01, verbose=False)[0]
output = postprocessor.process_ultralytics(
    result=result,
    camera_id="camera_001",
    timestamp=timestamp,  # 同一摄像头必须使用单调递增的秒时间戳
)

for event in output.events:
    verifier_queue.submit(event)
```

`output.events` 对同一个 `camera_id + track_id` 只产生一次。`output.tracks` 包含当前活动轨迹，`output.decisions` 给出每条轨迹未通过或通过门控的原因，可用于日志和参数调优。

如果摄像头重连、视频跳转或切换文件，应显式清除该摄像头状态：

```python
postprocessor.reset("camera_001")
```

当相邻两次推理时间差超过 `max_timestamp_gap` 时模块也会自动重置轨迹，避免断流前后的命中被错误累计。

## 参数含义

公共参数：

- `candidate_confidence`：进入跟踪器前的最低置信度。
- `max_missed_seconds`：轨迹在多久未命中后被删除。
- `ignore_regions`：按摄像头/类别配置的忽略区。

TPT 的 `classes` 参数：

- `window_size`：统计最近多少次小模型采样。
- `min_hits`：窗口内同一轨迹至少命中的次数。
- `min_track_duration`：轨迹最短持续时间。
- `max_misses`：窗口内允许的未命中次数。
- `high_confidence`：跳过严格时序等待的快速验证阈值。

AVT 的 `avt_classes` 参数：

- `window_size`：统计最近多少次有效面积观测。
- `area_threshold`：检测框面积变异系数阈值，计算方式为 `std(area) / mean(area)`。
- `min_samples`：计算 AVT 前至少需要的面积观测数。
- `min_track_duration`：AVT 允许触发前的最短轨迹时间。

默认 TPT 为火焰最近 3 次至少 2 次且持续 1 秒、烟雾最近 5 次至少 3 次且持续 2 秒。默认 AVT 参数与参考项目一致，面积变化阈值为 0.05、窗口大小为 20；额外要求至少两个面积样本，避免单样本统计无意义。



## 第二级大模型验证

可选的 MViT-v2-S 视频验证模块通过 `--verifier-weights` 启用；未提供权重时，现有 YOLO + AVT/TPT 行为不变。候选采集、标注清单、训练、评估和在线运行方法见 `大模型验证使用说明.md`。运行参数位于 `config/verifier.yaml`。

