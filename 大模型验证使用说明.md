# MViT-v2-S 第二级视频验证

## 处理链路

```text
YOLO -> AVT/TPT -> 3.2秒候选片段
                       |-- MViT-v2-S RGB时序特征
                       |-- rFFT时间频域特征
                       `-- 轨迹统计特征
                                  |
                         fire/smoke真实性头
                                  |
                        最近3个窗口至少2个阳性
                                  |
                                alarm
```

大模型输出的是候选目标的真实性概率 `p_real`。最终类别仍使用 YOLO 的 `fire` 或 `smoke`，不会把两个类别重新做成互斥三分类。

仓库不包含训练完成的大模型权重。`config/verifier.yaml` 中的 `0.5` 仅用于接口占位；实际运行优先使用训练检查点中按类别校准的阈值。

## 1. 采集候选片段

候选必须从真实的 `YOLO + AVT/TPT` 触发链路采集：

```bash
python run_temporal.py \
  --source input.mp4 \
  --candidate-dir verifier_candidates \
  --device 0
```

输出：

```text
verifier_candidates/
  manifest.jsonl
  samples/*.npz
```

系统按 0.5 秒生成滑动窗口。新记录默认是：

```json
{"sample":"samples/camera_001_1_000000003200.npz","source_id":"input.mp4","split":"unlabeled","candidate_class":"fire","label":null,"license":null,"source_duration_seconds":60.0}
```

必须人工确认片段后填写：

- `label`：真实火焰/烟雾为 `1`，困难误报为 `0`；
- `split`：`train`、`val` 或 `test`；
- `license`：数据授权或许可标识，训练代码拒绝许可为空的有标签数据；
- `source_id`：保留原始视频或摄像头事件来源。

同一 `source_id` 只能出现在一个数据划分中。代码会拒绝跨划分泄漏，避免同一视频的重叠窗口同时进入训练集和验证集。

## 2. 训练

生产模型：

```bash
python train_verifier.py \
  --manifest verifier_candidates/manifest.jsonl \
  --backbone mvit_v2_s \
  --mode fusion \
  --output weights/video_verifier.pt \
  --batch-size 4 \
  --device cuda:0
```

训练分两阶段执行：

1. 冻结 MViT 骨干，只训练频域支路、轨迹支路、融合层和分类头；
2. 解冻最后两个 MViT block，以较小学习率微调。

默认会获取 TorchVision 的 Kinetics-400 预训练权重。商用前必须审核代码、预训练权重及所有训练数据的许可。离线环境或结构测试可加 `--no-pretrained`。

对照实验：

```bash
# R3D-18 基线
python train_verifier.py --manifest MANIFEST --backbone r3d_18 --mode fusion --output weights/r3d18.pt

# 仅RGB时序分支
python train_verifier.py --manifest MANIFEST --mode rgb --output weights/mvit_rgb.pt

# 仅频域与轨迹分支
python train_verifier.py --manifest MANIFEST --mode frequency --output weights/frequency.pt
```

5/10 FPS 和不同 ROI 扩展比例实验通过修改 `config/verifier.yaml` 后重新采集同一组来源视频完成。不能只修改训练张量而保留旧频谱，因为这会改变频率含义。

## 3. 评估

```bash
python evaluate_verifier.py \
  --manifest verifier_candidates/manifest.jsonl \
  --weights weights/video_verifier.pt \
  --split test \
  --device cuda:0 \
  --output verifier_metrics.json
```

输出包括：

- 片段级 Precision、Recall、F1、PR-AUC；
- fire/smoke 分类别阈值和固定 95% Recall 下的 FPR；
- 2/3 窗口投票后的严格轨迹事件 Precision、Recall 和 FPR；
- 仅有视频级弱标签时，额外输出按原视频聚合的 `source_proxy_metrics`；
- 有源视频时长时的每小时误报警数；
- 平均报警延迟和重复报警率。

现有 49 个视频已经参与前级参数选择，只能用于域内验证。最终 test 必须使用未参与 YOLO、AVT 和大模型调参的新视频。

## 4. 在线验证

```bash
python run_temporal.py \
  --source input.mp4 \
  --model yolo26s-p2-1024-recall/weights/best.pt \
  --config config/temporal.yaml \
  --verifier-config config/verifier.yaml \
  --verifier-weights weights/video_verifier.pt \
  --verifier-device cuda:0 \
  --events events.jsonl
```

通过门控后，YOLO 推理频率临时提升到 10 FPS。每个验证窗口输出 `message_type=verification`；最近三个窗口至少两个阳性后输出 `message_type=alarm`。

跟踪器使用短时运动预测，并允许约 1.2 秒检测失配。验证会话额外保留最后 ROI 1.25 秒；新轨迹与旧 ROI 重合时会继承已有投票。同一摄像头同类别在 10 秒内只确认一次报警，同一区域在空间冷却期内也不会重复验证。被去重的门控事件输出 `message_type=verification_suppressed` 和 `reason=alarm_cooldown`。`tracker.max_misses` 控制轨迹身份保持，`classes.*.max_misses` 只控制 TPT 门控判定，二者含义不同。

如果源视频低于 8 FPS、有效帧不足或帧间隔过大，`quality.frequency_valid` 为 `false`，频域输入被置零，模型退化到 RGB 时序与轨迹特征。系统不会通过复制帧制造虚假的频谱。
