# 火灾烟雾检测系统端到端测试报告

## 1. 测试范围

测试日期：2026-08-24。

本次测试直接读取 `dataset` 中的原始视频，运行完整链路：

```text
视频解码
→ YOLO（1024，正常 5 FPS）
→ 分类目标跟踪
→ AVT 或 TPT
→ 3.2 秒轨迹 ROI
→ MViT-v2-S + rFFT + 轨迹统计
→ 最近 3 个窗口至少 2 个阳性
→ 报警冷却与去重
```

数据包括：

| 数据集 | 正视频 | 负视频 | 合计 |
|---|---:|---:|---:|
| fire_videos.1406 | 11 | 16 | 27 |
| smoke_videos.1407 | 13 | 9 | 22 |
| 总计 | 24 | 25 | 49 |

视频总时长约 1756.07 秒，其中负视频约 890.14 秒。测试使用 RTX 3090，49 个视频均成功完成，二级验证错误为 0。

这里的“正/负”是目录提供的视频级弱标签。测试没有逐帧或逐事件标注，因此本报告使用“目标类别是否至少报警一次”作为视频级代理指标。

## 2. AVT 默认方案

运行命令：

```bash
python evaluate_system.py \
  --dataset dataset \
  --temporal avt \
  --output evaluation_results/system_dataset_avt.json \
  --transcode-cache evaluation_results/transcoded_cache \
  --device 0 \
  --verifier-device cuda:0 \
  --resume
```

结果：

| 范围 | TP | FN | FP | TN | Precision | Recall | FPR | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 总体 | 22 | 2 | 1 | 24 | 0.9565 | 0.9167 | 0.0400 | 0.9388 |
| fire | 10 | 1 | 0 | 16 | 1.0000 | 0.9091 | 0.0000 | 0.9630 |
| smoke | 12 | 1 | 1 | 8 | 0.9231 | 0.9231 | 0.1111 | 0.9091 |

运行统计：

```text
门控事件：114
二级验证窗口：317
阳性窗口：165
频域有效窗口：239（75.39%）
报警事件：53
非数据集目标类别报警：9
重复验证抑制：32
验证错误：0
逐视频平均的验证器推理均值：46.07 ms
49 个独立子进程累计墙钟时间：296.65 秒
```

视频级首次目标报警平均时间为 5.57 秒。负视频中出现 1 次目标类别误报警，折算为 4.04 次/负视频小时；负视频总时长不足 15 分钟，因此该小时率方差很大，不能外推到真实摄像头。

## 3. TPT 对照方案

运行命令：

```bash
python evaluate_system.py \
  --dataset dataset \
  --temporal tpt \
  --output evaluation_results/system_dataset_tpt.json \
  --transcode-cache evaluation_results/transcoded_cache \
  --device 0 \
  --verifier-device cuda:0 \
  --resume
```

结果：

| 范围 | TP | FN | FP | TN | Precision | Recall | FPR | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 总体 | 20 | 4 | 1 | 24 | 0.9524 | 0.8333 | 0.0400 | 0.8980 |
| fire | 10 | 1 | 0 | 16 | 1.0000 | 0.9091 | 0.0000 | 0.9630 |
| smoke | 10 | 3 | 1 | 8 | 0.9091 | 0.7692 | 0.1111 | 0.8182 |

TPT 产生 85 个门控事件和 241 个验证窗口，相比 AVT 分别减少约 25.4% 和 24.0%，但总体 Recall 下降 8.33 个百分点，主要损失来自 smoke 正视频。因此当前仍推荐 AVT 作为默认门控，TPT 保留给资源更紧张或更强调少调用的场景。

## 4. 错误分析

AVT 的两个漏报视频：

| 视频 | 根因 |
|---|---|
| `fire_videos.1406/pos/posVideo7.876.avi` | YOLO fire 最高置信度约 0.135，低于当前 0.60 候选阈值，未进入跟踪和门控 |
| `smoke_videos.1407/pos/testpos05.821.avi` | 147 个 smoke 原始框中只有 6 个超过 0.20；形成的短轨迹面积变化不足，AVT 未触发 |

唯一目标类别误报：

```text
smoke_videos.1407/neg/testneg02.808.avi
```

该视频的三个 MViT 窗口分数约为 `0.9785、0.7773、0.8159`，smoke 阈值为 `0.7931`，因此满足 2/3 阳性。该错误位于二级验证模型，而不是门控或指标汇总。

TPT 除上述两个漏报外，还漏掉：

```text
smoke_videos.1407/pos/testpos06.822.avi
smoke_videos.1407/pos/testpos07.823.avi
```

## 5. 媒体兼容修复

数据集中包含 `rawvideo` 和 `indeo5` AVI。当前 OpenCV 5 后端读取部分文件时会产生原生内存错误并直接终止进程，Python 无法捕获。

系统现已在打开本地视频前使用 `ffprobe` 检查编码。对已知不安全编码，先通过 FFmpeg 转为 H.264 临时文件或缓存文件，再交给 OpenCV。原始 dataset 文件不会修改或删除。

单视频运行可复用缓存：

```bash
python run_temporal.py \
  --source dataset/fire_videos.1406/neg/negsVideo10.1072.avi \
  --transcode-cache evaluation_results/transcoded_cache \
  --verifier-weights weights/mvit_v2_s_fire_smoke.pt \
  --device 0 \
  --verifier-device cuda:0
```

## 6. 结论与下一步

本次结果支持继续使用当前默认配置：

```text
AVT
fire candidate conf = 0.60
smoke candidate conf = 0.20
MViT 检查点分类别阈值
2/3 窗口投票
```

但这 49 个视频已经参与训练或参数选择，且只有视频级弱标签，所以结果只能用于域内回归，不能作为最终验收指标。

下一阶段应优先：

1. 收集未参与训练和调参的新摄像头视频；
2. 增加 fire/smoke 困难负样本并逐片段人工标注；
3. 针对 `posVideo7.876` 研究高召回候选通道，而不是直接全局降低 fire 阈值；
4. 将 `testneg02.808` 一类困难 smoke 负片段加入训练；
5. 在独立验证集上重新校准 fire/smoke 的 MViT 阈值；
6. 测量单进程多摄像头队列下的端到端 P95 延迟。

